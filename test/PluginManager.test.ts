// import Plugin from '../src/api/Plugin';
// import PluginManager from '../src/api/PluginManager';

/**
 * Foo test
 */
describe('Foo test', () => { // eslint-disable-line @typescript-eslint/explicit-function-return-type

    it('works if true is truthy', () => { // eslint-disable-line @typescript-eslint/explicit-function-return-type
        expect(true).toBeTruthy();
    });

    // it('Foo is instantiable', () => { // eslint-disable-line @typescript-eslint/explicit-function-return-type
    //     expect(new PluginManager(new Plugin())).toBeInstanceOf(PluginManager);
    // });
    //
    // it('Foo dump works', () => { // eslint-disable-line @typescript-eslint/explicit-function-return-type
    //     new PluginManager(new Plugin()).dump();
    // });
});
